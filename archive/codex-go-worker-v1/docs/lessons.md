
- native tool probe: forced tool_choice returned HTTP400 Thinking mode does not support this tool_choice. Retry with automatic tool choice as Codex uses; no provider/model change.
- launcher static config gate: `codex-go --strict-config features list` exited 1 because Codex does not support strict-config on features. Rechecked with supported `codex-go features list`, which exited 0 and parsed the overlay. No model request was made.
- gateway pairing rule: two parallel image tool outputs with an interleaved `<image_resize_notice>` developer message returned HTTP400 `No tool output found for tool call`. Moving notices after all outputs, or serializing the calls, returns 200. Reproduced synthetically (T1/T2/T4/T5) but not naturally on current clients.
- client session header: Codex 0.153.4/0.154.0 already send `Session-Id`/`Thread-Id` on `/responses`. A static `x-opencode-session` provider override was added, verified as transmitted, then removed because it would collapse per-conversation routing into one id.
- `-c` placement: overrides passed after the subcommand (`codex-go exec -c 'model=…'`) drop the launcher's `model_provider` overlay; the request goes to OpenAI and fails with "model is not supported when using Codex with a ChatGPT account". Put `-c` before `exec`.
- catalog expansion: added `deepseek-v4-pro`, `deepseek-v4-flash`, `deepseek-v4-flash-vision-exp` to `models.json`; both v4-flash and v4-pro completed real shell tool round-trips through the Go gateway.
- 2026-09-11 16:28 probe (my mistake): 给 Go 的 `agents/luna` 设成 `deepseek-v4-pro` 又派了一次真实 spawn 验证，子 agent 以 `fork_turns=all` 继承整段上下文，1 分钟内开出 6 个 v4-pro 子会话（01a08f95-5239 … 01a08f96-1b88），计费 US$0.4675。检查项：Go 侧任何路径只允许 `deepseek-flash`。失败值：`agents.luna.model="deepseek-v4-pro"`。根因：把「验证开关是否生效」写成「用真实计费调用验证」，且给 Go 选了非 Flash 模型。修正：Go 的 luna variant 与 `-c` 覆盖都改回 `deepseek-flash`，`models.json` 只留 `deepseek-flash`，不再用真实调用做验证。
- Luna 计费根因（2026-09-11 13:15 实测）：记忆摄取由持有 Go provider 的进程发起，phase1 固定用 `gpt-5.6-luna`，因此打到 `opencode.ai/zen/go/v1` 并由 Go 套餐计费。13:15:24 一次性 18 个请求（cf_ray `a39431c9ea15297d-SEA`），来自 pid 23617（只处理 opencode_go 会话，13:22:14 退出）。`memories.extract_model` 在进程启动时设置有效（pid 74092：517 次全部 deepseek-flash，零 Luna），因此 Go 侧必须显式钉住记忆模型，并让 Go 会话不再进入摄取候选。
